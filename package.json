{
  "name": "zyber",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "bun run --filter='*' dev",
    "typecheck": "tsc -b && tsc --noEmit -p apps/web/tsconfig.json"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5"
  }
}
